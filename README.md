# Trains Lambda

This is a function for AWS Lambda which gets a list of train departures for the selected station and outputs them into a 32-column plain text format for printing on a thermal printer.

Data comes from the [Realtime Trains](https://www.realtimetrains.co.uk) API.